# 0169. Majority Element

**Problem Name:** Majority Element  
**Difficulty:** Easy

---

## Question

Given an integer array `nums` of size `n`, return the majority element.

The majority element is the element that appears more than `⌊n / 2⌋` times.

You may assume that the majority element always exists in the array.

---

## My Solution

```cpp
class Solution {
public:
    int majorityElement(vector<int>& nums) {

        int count = 0;
        int candidate = 0;

        for (int i = 0; i < nums.size(); i++) {

            if (count == 0) {
                candidate = nums[i];
            }

            if (nums[i] == candidate) {
                count++;
            }
            else {
                count--;
            }
        }

        return candidate;
    }
};
```

---

## Complexity

| Metric | Value |
|--------|-------|
| **Time Complexity** | `O(n)` |
| **Space Complexity** | `O(1)` |

---

## What I Learned

This problem introduced me to the **Boyer-Moore Voting Algorithm**, an efficient way to find the majority element without using extra memory.

The algorithm works by maintaining a candidate and a counter. Whenever the counter becomes zero, a new candidate is selected. Matching elements increase the counter, while different elements decrease it.

Since the majority element appears more than half of the time, it will always remain as the final candidate.

---

## Personal Notes

Initially, I thought about using a hash map to count the frequency of each element. Although that approach is straightforward, it requires additional space.

Learning the Boyer-Moore Voting Algorithm showed me that some problems have elegant solutions based on simple observations rather than extra data structures.

This was my first time using this algorithm, and it helped me understand how maintaining a candidate and balancing counts can solve the problem in linear time with constant space.

---

**Status:** Solved ✅
