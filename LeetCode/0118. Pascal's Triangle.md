# 0118. Pascal's Triangle

**Problem Name:** Pascal's Triangle  
**Difficulty:** Easy

---

## Question

Given an integer `numRows`, return the first `numRows` of Pascal's Triangle.

In Pascal's Triangle, every number is the sum of the two numbers directly above it, except for the first and last element of each row, which are always `1`.

---

## My Solution

```cpp
class Solution {
public:
    vector<vector<int>> generate(int numRows) {

        vector<vector<int>> ans;

        for (int i = 0; i < numRows; i++) {

            vector<int> row(i + 1, 1);

            for (int j = 1; j < i; j++) {
                row[j] = ans[i - 1][j - 1] + ans[i - 1][j];
            }

            ans.push_back(row);
        }

        return ans;
    }
};
```

---

## Complexity

| Metric | Value |
|--------|-------|
| **Time Complexity** | `O(n²)` |
| **Space Complexity** | `O(n²)` |

---

## What I Learned

This problem helped me understand how to build a two-dimensional vector step by step.

I learned that the first and last elements of every row are always `1`, while the remaining elements can be calculated using the previous row.

It also gave me more practice with nested loops and working with 2D vectors.

---

## Personal Notes

At first, I wasn't sure how each row was generated. After observing the pattern, I realized that every row depends only on the row before it.

Instead of memorizing Pascal's Triangle, I focused on understanding how each value is formed. This made the implementation much easier and helped me become more comfortable working with matrices and nested vectors.

---

**Status:** Solved ✅
